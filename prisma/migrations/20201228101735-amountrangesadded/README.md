# Migration `20201228101735-amountrangesadded`

This migration has been generated by nouman at 12/28/2020, 3:17:35 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;
CREATE TABLE "new_WatchSymbols" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "symbol" TEXT NOT NULL,
    "minAmount" TEXT NOT NULL DEFAULT '',
    "maxAmount" TEXT NOT NULL DEFAULT '',
    "phone" TEXT NOT NULL DEFAULT '',
    "createdAt" DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "postedById" INTEGER,

    FOREIGN KEY ("postedById") REFERENCES "User"("id") ON DELETE SET NULL ON UPDATE CASCADE
);
INSERT INTO "new_WatchSymbols" ("id", "createdAt", "postedById", "symbol", "phone") SELECT "id", "createdAt", "postedById", "symbol", "phone" FROM "WatchSymbols";
DROP TABLE "WatchSymbols";
ALTER TABLE "new_WatchSymbols" RENAME TO "WatchSymbols";
CREATE TABLE "new_sms" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "msg" TEXT NOT NULL DEFAULT '',
    "phone" TEXT NOT NULL DEFAULT '',
    "createdAt" DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "sent" BOOLEAN NOT NULL DEFAULT false,
    "onceDaily" TEXT NOT NULL
);
INSERT INTO "new_sms" ("id", "phone", "createdAt", "sent", "onceDaily") SELECT "id", "phone", "createdAt", "sent", "onceDaily" FROM "sms";
DROP TABLE "sms";
ALTER TABLE "new_sms" RENAME TO "sms";
CREATE UNIQUE INDEX "sms.onceDaily_unique" ON "sms"("onceDaily");
PRAGMA foreign_key_check;
PRAGMA foreign_keys=ON
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201226132911-smstableadded..20201228101735-amountrangesadded
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource db {
     provider = "sqlite"
-    url = "***"
+    url = "***"
 }
 generator client {
@@ -38,9 +38,10 @@
 model WatchSymbols {
     id          Int          @id @default(autoincrement())
     symbol      String
-    amount      String       @default("")
+    minAmount      String       @default("")
+    maxAmount      String       @default("")
     phone       String       @default("")
     createdAt   DateTime     @default(now())
     postedById  Int?
     postedBy    User?        @relation(fields: [postedById], references: [id])
@@ -49,10 +50,9 @@
 model sms {
     id          Int          @id @default(autoincrement())
-    symbol      String
-    amount      String       @default("")
+    msg         String       @default("")
     phone       String       @default("")
     createdAt   DateTime     @default(now())
     sent        Boolean      @default(false)
     onceDaily   String       @unique
```


